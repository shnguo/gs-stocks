**全 A 股 Transformer 量化研究开发计划**

## 异常验收原则更新（2026-09-10）

按用户确认，异常以结果可信、证据可保存和独立复核为验收标准，公司公告不是所有异常的必需条件。可使用原始行情参考价、不同供应方因子事件、已实施分配记录及其数量关系进行交叉核对。不同网站可能共用上游，供应方不同不自动等于独立来源；数值冲突未解释时继续待核。证据只能支持其已验证结论：价格连续性通过不等于实际现金分红、送转股或配股权益已确认，后者仍需对应权益证据，但证据形式不限于公告。新验收结果进入新的数据版本，正在执行的训练使用其原冻结快照。


编制日期：2026-09-07（北京时间）。版本：v5，已启动全市场历史采集与可恢复任务队列，允许 ST 纳入训练。当前目录来自 9 月 7 日的完整上市主表，覆盖沪深北 5,558 个成员；历史代码候选 5,901 条，其中 5,812 条在研究区间内，共 11,624 个采集任务。已采约 32 万条原始日线/停牌记录并复用到新版队列，身份映射和完整数据验收仍待完成。当前实现与缺口见 [开发状态](quant-research/STATUS.md)、[完整股票池](quant-research/docs/full-market-pool.md) 和 [采集恢复记录](quant-research/docs/full-market-backfill.md)。此前八股运行仅为工程测试，真实全市场研究和 P5–P7 验收尚未完成。

目标是在现有行情系统上建立可复现的日频选股研究流程，验证小型 Transformer 对简单因子和 LightGBM 的增量价值，交付每日股票排序、受约束的模拟组合以及可追溯的样本外报告。研究开发预计 6–8 周；外部数据权限、历史补齐的等待时间另计，模拟盘观察另安排至少 40 个交易日。

**已确定的研究范围如下，技术参数采用本文列出的初始值，并在 P0 冻结。**

| 项目 | 开发口径 | 决策来源 |
|---|---|---|
| 股票范围 | 现有证券主库中的沪、深、北 A 股；保留主板、科创板、创业板、北交所 | 用户已确认全 A 股 |
| 训练中的 ST | 纳入 ST、*ST 和风险状态未知样本；已有状态独立留痕，不作为首版特征 | 用户最新允许 ST 纳入训练 |
| 模拟买入中的 ST | 按历史当时已知状态剔除 ST、*ST 和未知状态 | 保留原有非 ST 交易范围，训练与交易分别控制 |
| 任务 | 多股票横截面排序 | 用户已确认 |
| 预测周期 | 5 日为主实验，20 日为独立对照实验 | 用户确认持有约 5–20 日；实验顺序为建议 |
| 数据频率 | 日频，使用已完成交易日的数据 | 本轮初始方案 |
| 策略形式 | 多头组合，现金余额允许保留 | 本轮初始方案 |
| 信号与成交 | 暂定北京时间 T 日 20:00 固定信息截面，T+1 日开盘尝试执行 | P0 验证数据时效并冻结 |
| 调仓 | 主实验每 5 个交易日调仓，对照每 20 个交易日调仓；每日更新评分和风险状态 | 本轮初始方案 |
| 网络 | 共享权重的小型时序 Transformer，先分别训练两个周期 | 本轮初始方案 |
| 第一阶段交付边界 | 离线研究、历史回测、模拟持仓、研究结果展示 | 本计划范围 |

“全 A 股”指研究证券范围，必须由独立市场证券目录定义，禁止使用个人自选列表或已下载行情列表替代。日内或每日无法评分、无法成交的证券仍须出现在覆盖台账中，并写明原因；不能通过删除难处理的股票改善回测结果。默认训练入口检查全池覆盖，不完整的小样本仅允许通过明确的工程测试入口运行。

1. **从现有系统能复用什么开始。**

初始规划核验覆盖本地代码、数据合同、路线图和公开接口文档；实施阶段已补充 Tushare 与 D1 抽查，详见数据审计。全量历史范围、退市股覆盖和数据质量仍需逐行验证。文档或代码中的“已实现”不代表线上多年数据已经齐备。

| 已核验的本地基础 | 对开发的帮助 | 仍需验证或补齐 |
|---|---|---|
| 证券与上市区间记录含稳定 ID、代码、生效区间和来源修订 | 构建历史证券池、处理代码变化 | 全量历史上市/退市区间、板块变更和源记录覆盖 |
| 历史 bootstrap 已有按证券与时间窗口规划、覆盖状态和可恢复流程 | 优先复用历史采集与补齐能力 | 多年日线和公司行动是否实际发布、是否存在无法获取的区间 |
| 行业归属合同含生效区间与来源血缘 | 做历史行业特征和风险暴露归因 | 当时可得时间、历史版本和覆盖完整度 |
| 日线与复权合同保留价格口径、来源、修订和原始对象 | 导出固定数据版本，重跑实验 | 分红拆股处理、复权因子的历史可得性 |
| 当前网页图表路由固定请求 400 个自然日窗口、最多 250 根日线、latest 模式 | 可继续作为图表展示路径 | 多年训练需要独立的研究导出路径 |
| 历史 ST 尚未完整接入，已有部分名称变更候选 | 将 ST 作为辅助审计与非 ST 回测依赖 | ST/撤销 ST 的历史事件、日状态、完整性与权限；不阻挡包含 ST 的训练池构建 |

本地依据：[上市区间模型](/Users/guo/github/mootdx-cf/crates/lakehouse-reference-data/src/lib.rs:109)、[历史证券范围构建](/Users/guo/github/mootdx-cf/crates/lakehouse-history/src/lib.rs:589)、[历史数据路线图](/Users/guo/github/mootdx-cf/A_SHARE_HISTORY_ROADMAP.md)、[行业归属合同](/Users/guo/github/mootdx-cf/contracts/industry-memberships.v1.json)、[图表接口](/Users/guo/Documents/stocks/stock-indicator-dashboard/app/api/chart-daily-bars/route.ts:41)。

历史范围构建目前按来源优先级和抓取时间选取修订。研究导出必须进一步定义“当时已经公开的信息”与“后来抓取、重建的信息”的边界，核实每个数据集的版本选择规则。有效日期区间和快照创建时间本身，不能证明没有未来信息。

2. **建立研究数据合同，优先补齐历史证券池和行情。**

| 数据集 | 最低必要字段与语义 | 首版用途 |
|---|---|---|
| 证券、上市与代码区间 | instrument_id、交易所、板块、代码有效期、上市/退市区间、来源修订 | 确定各日应覆盖的 A 股 |
| 交易日历 | 各交易所实际交易日、前后交易日、特殊安排及版本 | 样本窗口、标签到期和调仓日 |
| 风险警示状态 | normal、ST、*ST、unknown；公告时间、生效时间、撤销时间、来源与完整性 | 训练审计与分组评估、非 ST 模拟买入过滤和执行前风控；缺少时不阻挡训练样本 |
| 不复权日线 | OHLC、真实成交股数、成交额、日期、来源、质量状态 | 成交模拟、收益和量价特征 |
| 复权与公司行动 | 因子及口径、公开/生效时间、分红、拆并股等事件 | 跨公司行动的收益、持仓数量与现金记账 |
| 交易状态与限制 | 停复牌、每日适用价格限制、成交单位、可卖数量规则、制度生效区间 | 无法成交、持仓延迟和历史规则模拟 |
| 行业、指数与规模 | 历史行业归属、截至当时的指数数据、可用的流通规模 | 市场状态特征、组合约束和风险归因 |
| 覆盖与质量台账 | 应有记录、实际记录、缺口原因、隔离原因、修订和原始对象 | 每次实验的覆盖报告 |

Tushare 的 stock_st 文档提供按交易日期查询历史 ST 列表的接口；st 文档提供公告日期、实施日期和风险警示变更类型。它们是候选来源，需要先验证现有账号权限、分页完整度、沪深北覆盖及撤销事件语义，再决定接入方式。文档同时提示早期历史可能不完整，不能把空响应直接解释为当日没有 ST。[历史 ST 列表文档](https://tushare.pro/document/2?doc_id=397)、[风险警示事件文档](https://tushare.pro/document/2?doc_id=423)

训练与模拟买入采用独立规则：

- 训练、验证和排序测试纳入 ST、*ST 与未知状态，只按上市区间、历史行情、特征窗口和标签条件判断样本是否可用。ST 状态不改变排名标签或首版特征。
- 只有来源返回完整日状态快照，或可靠事件区间能够证明状态时，才允许判定为非 ST。未知状态原样保留，允许训练，但不进入非 ST 模拟买入池。
- 后来变为 ST、摘帽或名称数据缺失，均不删除可用训练样本；样本持有期内的亏损继续保留，资格不依赖未来风险事件。
- 在 T+1 实际执行前重新核对已知风险事件；已生效 ST 的新增买单取消，已有持仓进入待退出状态。公告时间、生效时间和实际可成交时间分别记录。
- 持仓期间变为 ST 后，于生效后的下一可成交时点退出；停牌或卖出受阻时继续记账，保留风险暴露、现金变化和退出延迟。
- 撤销风险警示后，自生效日起重新检查模拟买入资格。训练与评分不因 ST 状态本身中断。
- 历史名称变更可做辅助核对；仅用当前名称是否含 ST，无法满足历史过滤要求。交易所规则中的风险警示以实施事件为准。[上交所上市规则](https://www.sse.com.cn/lawandrules/sselawsrules2025/bond/convertible/listing/c/c_20260424_10817746.shtml)

历史证券池纳入研究期间曾经上市的 A 股，包括后来退市证券的有效历史。证券主库如果仅有当前上市股票，必须先补历史证券范围，不能直接宣称完成无幸存者偏差的全 A 股回测。北交所证券代码变化使用稳定证券 ID 和有效代码区间衔接，不能仅按当前代码前缀重建全部历史。

训练数据应争取覆盖至少约 10 年及不同市场状态；每只股票仅要求满足其上市后的实际样本条件，不要求所有股票上市满 10 年。北交所等板块按实际历史存在区间覆盖。若暂时只有 250 根日线，可以完成工程贯通，但不足以完成本计划的多年研究验收。

研究导出固定 dataset_id、证券范围版本、各数据集快照版本、来源策略、信息可得规则、导出时间、内容哈希与代码版本。来源的 published_at、事件的 effective_at、系统的 fetched_at 分别保存。历史回补数据若缺少当时版本证据，明确标注为“事后重建历史”；不得补造历史公开时间。与此相关的版本选择可参考 Qlib 的点时数据设计，但引入框架不会自动补齐历史事实。[Qlib 点时数据说明](https://qlib.readthedocs.io/en/stable/advanced/PIT.html)

3. **把标签、特征和预测时间一次定义清楚。**

所有日期使用交易所交易日历，交易日业务时间使用 Asia/Shanghai，记录时间同时保存 UTC。暂定 T 日 20:00 为信号截面：只使用已收盘、已通过质量检查且在该截面前可得的数据。若关键输入在截止时刻未就绪，该日不新增风险仓位，保留原因和持仓风控日志；禁止在次日开盘后补跑并追记为开盘前信号。P0 根据实际数据时效冻结批次时间。

标签定义为下一交易日开盘起算的固定周期持有收益，减去同区间、由 T 日证券范围固定的市场基准收益。收益采用包含适用公司行动的财富变化，训练标签不扣某个特定资金规模的交易成本；实际扣费收益在组合回测中核算。

| 任务 | 计划入场时点 | 标签终点 | 主对照调仓间隔 |
|---|---|---|---|
| 5 日排序 | T+1 开盘 | T+6 开盘 | 5 个交易日 |
| 20 日排序 | T+1 开盘 | T+21 开盘 | 20 个交易日 |

原始收益、超额收益和同日排序标签分别保留。训练起点采用横截面百分位映射到 -0.5 至 0.5 的排序目标及 Huber 损失，输出评分不解释成收益百分比或上涨概率。市场共同收益的扣除不会自动消除行业、市值和 beta 暴露；风险归因和组合约束仍单独实施。

未来期间发生 ST、停牌或退市的样本需要保留其真实结果。没有可核验终点价值的标签标为未解决，展示分布与数量；不能直接填零或无说明删除。账户回测继续跟踪这些持仓，区分最后可观察估值和可变现价值，对未解决终值给出压力情景。固定周期标签与风控提前退出后的实际收益分别报告。

只对已经成熟、结果已知的标签训练。每次训练记录 label_available_at；数据末端尚未到达标签终点的样本可进行实时评分，但不用于监督训练或已实现效果统计。

首版特征控制在约 20–40 个，最终名单及定义在 P0/P2 冻结：

- 相对价格与收益：开收盘关系、前收盘相对变化、实体与振幅、1/5/10/20/60 日收益等。
- 量与流动性：真实成交量和成交额的相对变化、滚动均值比、可核验的换手信息；所有单位显式声明。
- 波动与趋势：历史波动、均线偏离、回撤等少量统计量。
- 市场背景：截至当时的市场和行业收益、市场波动及板块类别。
- 缺失与停牌标记：区分真实停牌、缺失、未上市和来源失败，禁止在原始行情层伪造蜡烛。

输入序列为 60 个市场交易日。每项滚动特征声明所需预热长度；例如每个时间步都需要 60 日统计时，完整的 60 步输入最多需要 119 个市场交易日的数据。上市早期样本因此逐日标记为窗口不足，不能把这些证券从全历史名单中删除。停牌时间步使用显式掩码和既定缺失处理，不压缩交易时间轴。

特征可在研究层从可靠的公司行动与因子重建收益，成交价格必须保留不复权口径。未来公司行动可以进入标签结果和现金账，不得进入早期特征。禁止把当前重算的前复权价格绝对水平直接当作历史已知信息。

所有需要估计的标准化、截尾、填补和特征选择参数仅在训练折拟合；使用当日已知特征做横截面转换时，逐日计算并明确该日证券范围。验证、测试和模拟推理使用同一处理实现。

4. **把模型比较拆成有限的实验。**

| 实验 | 内容 | 目的 |
|---|---|---|
| E0 | 简单动量/反转等预先固定的少量因子、等权参考组合 | 核对数据、交易账和基本收益归因 |
| E1 | LightGBM，使用相同信息范围的量价统计特征 | 建立第一条有竞争力的基线 |
| E2 | 小型 Transformer，60 步量价序列，5 日标签 | 主模型实验 |
| E3 | 同一结构、独立训练的 20 日模型 | 比较周期与换手成本 |
| E4 | 基线与 Transformer 评分的固定权重组合 | 检验信号互补性；权重只在验证数据选取 |

E1 与 E2 的基础比较共享数据来源、历史信息跨度和目标，但不宣称输入表达完全相同。若要归因到网络结构，增加一个受限的“相同 60 步序列展开为表格特征”的 LightGBM 对照，并单独记录维度和计算预算。

Transformer 初始配置：2 层、隐藏维度 128、4 个注意力头、前馈维度 256、dropout 0.1、位置编码和一个评分头。每只股票独立做历史时序编码，全市场共享权重；第一轮不依赖全市场股票之间的注意力。首版不加入永久股票 ID 嵌入，减少对股票身份和存续样本的记忆。

训练与指标按日期组织，控制每个交易日的权重，避免股票数量较多的日期天然占更大损失权重。单日可以分块前向计算；Huber 初始目标无需一次把全市场放入显存。按日排序损失作为后续消融，采样和分块方式必须保持同日语义，不能把跨日期样本放在一起排序。

初始优化方案：AdamW，学习率 0.0003，权重衰减 0.01，最多 30 个 epoch，验证指标连续 5 个 epoch 无改善停止；这些均为待验证初始值。P0 锁定开发实验预算，建议每个周期最多 8 套参数配置，选定配置后使用 3 个固定随机种子复核。实验失败和所有尝试均记录，不能只保存最好的一次。

MASTER 的股票关系建模、共享双周期预测头以及 Kronos 的预训练比较放入第二阶段候选。MASTER 官方披露过验证集预处理与 Qlib 版本差异；Kronos 提供微调流程，但其示例仍需补足真实组合和执行约束。预训练模型需要记录权重哈希、训练数据截止时间和测试期重叠风险，未知重叠时单列结果，并通过固定权重后的未来数据验证。[MASTER 官方说明](https://github.com/SJTU-DMTai/MASTER)、[Kronos 官方仓库](https://github.com/shiyu-coder/Kronos)

5. **先实现账户与成交账，再比较模型收益。**

主回测从固定模拟资金、最多 50 只等权目标持仓开始。建议初始名义资金为 100 万元，仅用于研究假设，不代表用户实际账户；P0 将资金值、个股上限、行业偏离上限、流动性规则和交易费用表写入配置。初始个股目标权重上限 2%，行业权重相对研究基准偏离上限 5 个百分点。受约束时允许现金留存，不为达到持股数放松限制。

流动性约束在组合与执行层处理：不预先删掉所有小盘或北交所股票。下单量先受过去 20 日成交量上限约束，默认研究起点为不超过过去 20 日平均日成交股数的 1%，按各板块有效成交单位取整。这个上限只是低参与度的初步约束，不能证明开盘就有足够流动性。

基础成交场景使用开盘价格加减预设不利滑点；对开盘价格限制、单边无法成交、停牌、股数规则、现金不足和可卖数量分别处理。仅有日线不能证明开盘队列中的实际成交：开盘处于买入方向上限或卖出方向下限时，基础场景保守判为未成交；即使全天后来打开，也不凭当日高低价假设提前成交。

同时提供延迟一日执行、成本加倍和更保守成交率的压力场景。若结果主要依赖对开盘成交的乐观假设，进入后续模拟验证前需要补集合竞价/分时证据。任何使用当日成交量或高低价的撮合约束都只能属于事后市场仿真，不得反过来决定开盘前的目标股票或订单数量。

交易费率、税费、价格限制、买卖单位和可卖数量规则均按交易所、板块及历史生效日期配置。实施前核对各交易所规则及拟用券商费率；不把当前规则机械应用到所有历史日期。上交所已发布 2026 年修订交易规则，进一步说明版本化规则表属于回测必要输入。[上交所交易规则通知](https://www.sse.com.cn/lawandrules/sselawsrules2025/trade/universal/c/c_20260424_10816492.shtml)

回测记录信号日、计划成交日、实际成交日、目标权重、订单股数、成交股数、费用、现金、持仓、未成交原因和风险事件。被 ST 的持仓强制退出不等待常规调仓日；卖不出时继续暴露风险。整组持仓每天记账，重叠持有期不能当作互相独立、各自拥有完整资金的账户叠加收益。

基础实验先固定换仓规则。较高排名进入、较低排名才退出的缓冲区作为后续单变量实验，避免同时改变网络、股票筛选和交易机制后无法解释收益来源。常规持有周期为 5/20 日；风控可能提前退出，成交受阻可能延长实际持有。

6. **用时间推进的验证决定是否继续。**

在历史覆盖满足条件时，初始方案使用 5 年训练、1 年验证、随后 1 年开发测试，按年向前推进，争取覆盖至少 3 个完整开发测试年度。最近另外 12 个完整月份的成熟样本作为最终封存测试区间。准确起止日期由 P0/P1 根据数据覆盖、交易日历和最长 20 日标签成熟时间生成并冻结，不把尚未成熟的近期样本硬填进测试。

开发测试用于迭代，必须披露反复查看造成的选择偏差；最终封存集只在模型、特征、规则和接受标准冻结后评估一次。之后若据此修改方案，该区间失去封存集资格，需用新的未来数据验证。不能把“只在验证集调参”作为反复查看终测的替代说法。

每次训练只读取当时已经成熟且已知的标签。剔除标签区间跨越训练/验证/测试边界的训练样本；隔离长度依据真实的标签起止与可得时间计算，不按面板总行数设置一个固定 gap。同一天的股票整体归入同一时间分区。验证和测试输入可以使用此前已经公开的历史价格；单纯输入窗口重叠不等同于标签泄漏。通用 TimeSeriesSplit 提供时间顺序切分工具，本项目仍需额外实现按日期分组和按标签区间清理。[scikit-learn 时间切分说明](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.TimeSeriesSplit.html)

每个模型按同一再训练计划运行。初始计划每季度重新训练一次，超参数在开发验证中确定，最终测试只执行冻结的再训练规则；再训练时禁止使用当时尚未成熟的最近标签。

| 验证维度 | 必须交付的证据 |
|---|---|
| 排序效果 | 每日 RankIC、分位组收益、正相关日期比例、年度与板块拆分 |
| 训练与交易范围差异 | 分列全部训练候选和非 ST 信号子集的 RankIC；ST、*ST、未知数量单列，组合收益只对应其实际执行规则 |
| 统计不确定性 | 按时间块重采样的区间估计；块长考虑 20 日重叠标签，并做块长敏感性核对 |
| 可交易收益 | 扣费净值、年化收益与波动、最大回撤、换手、现金比例和未成交统计 |
| 风险来源 | 行业、板块、规模、beta 暴露，以及剔除小盘收益贡献后的敏感性 |
| 基线增量 | 相同组合/成本规则下，Transformer 和融合模型相对 LightGBM 的逐日差值 |
| 稳健性 | 不同年度、3 个随机种子、成本加倍、成交延迟、不同调仓起点 |
| 样本诚实性 | ST、退市、缺失、未成熟标签的处理与覆盖差异；异常样本损失单列 |

7. **按里程碑开发，每阶段都有可审查的输出。**

| 阶段 | 预计工程时间 | 工作与交付物 | 完成条件 |
|---|---|---|---|
| P0：数据与规格审计 | 2 个工作日 | 实际股票数量、年度覆盖、ST/退市/行业/公司行动权限与质量报告；固定标签、资金、分区、规则和实验预算；研究工程设计 | 明确哪些字段真实可得，未知项有台账；时间与标签定义无歧义 |
| P1：全 A 股历史训练数据集 | 5–10 个工作日，外部等待另计 | 复用 Rust 数据层补齐必要合同/适配；构建日期证券池、研究导出与不可变 manifest；ST 候选独立留痕 | 上市身份、行情和标签可核验；ST 允许未知且不用于模型输入；能按来源重建快照 |
| P2：特征、标签与样本生成 | 4–5 个工作日 | 特征清单、预热规则、5/20 日标签、按日期切分及泄漏检查、统一数据加载器 | 人工样例与独立计算一致；未来数据扰动不改变早期特征和入池资格 |
| P3：基线与成交回测 | 4–5 个工作日 | E0/E1、现金与持仓账、ST 退出、未成交和成本模型、第一份基线报告 | 信号到成交全可追溯；账本守恒；在固定快照上可重跑 |
| P4：Transformer 实验 | 5–7 个工作日 | E2/E3、训练日志、模型权重、3 种子结果、必要的架构对照 | 全研究范围分批评分无重复/漏行；数据和预测合同与基线一致 |
| P5：增量与样本外评估 | 4–6 个工作日 | E4、开发年度对照、压力情景、消融与归因；方案冻结后一次终测 | 给出继续、仅保留基线、证据不足或停止的明确结论 |
| P6：每日模拟流程与展示 | 3–5 个工作日 | 每日快照就绪检查、评分、目标持仓、模拟执行、模型版本登记、研究页面 | 重跑幂等；迟到数据不伪造信号；缺失模型/数据有明确失败状态 |
| P7：模拟观察 | 至少 40 个交易日，独立于工程工期 | 每日信号与成交对账、延迟/失败恢复记录、真实预测成熟后的效果报告 | 验证操作流程与历史回测偏差；20 日策略的少量周期不能单独证明稳定盈利 |

依赖顺序为 P0 → P1 → P2 → P3 → P4 → P5 → P6 → P7。等待外部数据时可先用固定样例完成数据加载、账本和模型接口，但正式回测结果仍须等待 P1 验收。

预计第 1–2 周完成规格和首版数据集，第 3–4 周交付基线，第 5–6 周完成 Transformer 与开发对照，第 7–8 周完成最终评估和模拟运行接入。长历史行情、退市身份和公司行动是主要进度风险；完整 ST 历史不再是训练的前置依赖，仍影响非 ST 组合回测验收。

8. **实现分工与工程目录保持清楚。**

| 系统 | 职责 | 实现方式 |
|---|---|---|
| mootdx-cf | 原始数据保留、证券与 ST 规范化、历史修订、质量校验、可重放采集和研究导出合同 | 延续 Rust 生产数据逻辑，Cloudflare 适配保持轻量 |
| 新建研究工程 | 冻结快照的读取、特征/标签、LightGBM/PyTorch 训练、研究回测、实验报告与模拟验证 | 独立 Python 研究工程，锁定依赖与随机种子 |
| stock-indicator-dashboard | 模型版本、数据日期、评分排名、候选/排除原因、模拟持仓和历史报告展示 | 通过独立研究结果接口读取有版本的数据 |

这符合现有仓库的 Rust-first 约定：Python 用于独立研究，生产采集和金融规范化继续由 Rust 负责。[仓库开发约定](/Users/guo/github/mootdx-cf/AGENTS.md)

研究工程已建于 /Users/guo/Documents/stocks/quant-research。下表为模块职责规划，实际平铺模块与已完成情况见开发状态：

| 拟建模块或目录 | 责任 |
|---|---|
| configs | 股票范围、ST 规则、特征、标签、模型、成本、时间分区与实验预算 |
| manifests | 数据、证券范围、源版本、特征定义和模型权重的固定清单 |
| data | 经验证的本地列式文件与缓存；大数据与凭证不进入版本库 |
| src/quant_research/data | 读取研究导出、检查模式/哈希/覆盖 |
| src/quant_research/universe | 日期证券池、ST 与资格原因 |
| src/quant_research/features、labels | 一份统一的特征/标签实现与元数据 |
| src/quant_research/models | 因子基线、LightGBM、Transformer 的共同训练/预测接口 |
| src/quant_research/evaluation | 时间分区、标签清理、指标、时间块统计和归因 |
| src/quant_research/portfolio、execution | 目标权重、订单、现金持仓账、成交与风控 |
| src/quant_research/reporting | 数据覆盖、实验对照、模拟盘与异常报告 |
| tests | 数据时点、ST 事件、账本、模型输出合同与恢复行为测试 |
| artifacts | 权重、预测、组合、订单、报告和完整运行 manifest |

每条预测至少包含 signal_date、signal_cutoff、instrument_id、horizon、score、rank、universe_id、dataset_id、feature_version、model_id、run_id。不能评分的证券另有状态记录和原因；不能用零分冒充成功预测。信号、组合、订单和持仓使用统一运行 ID 串联。

每次实验固定代码版本、数据快照、配置哈希、依赖锁文件、硬件/后端、随机种子、训练窗口、早停依据、训练耗时与输出哈希。同环境重跑验证确定性；不同 CPU/MPS/CUDA 后端以事先声明的数值容差和排序一致性评估，不承诺天然逐位相同。

9. **用少量关键测试覆盖会改变结论的风险。**

| 测试情景 | 必须成立的行为 |
|---|---|
| T 日正常，T+3 才公告或实施 ST | T 日入池和训练样本不因未来事件被删除；后续账户按事件处理 |
| ST 公告在信号后、下一次成交前已可得并生效 | 新买单取消，已有持仓记录退出尝试及受阻原因 |
| ST 后停牌或连续卖出受阻 | 账户保留持仓和风险，不能按最后价格假装现金退出 |
| 撤销 ST、生效时间变化或历史修订 | 各日期资格正确，历史数据版本选择可追溯 |
| ST 来源空响应、分页截断、日状态不完整 | 标记 unknown/失败；仍可训练，不把缺少记录当作非 ST 或允许模拟买入 |
| 训练纳入 ST 与未知状态 | 特征、标签、日期切分可复核；不能放宽交易过滤、上市区间或行情质量要求 |
| 退市、代码变化、新上市、行业变更 | 历史证券身份正确，不能套用今天的股票池或行业 |
| 除权除息、拆股、分红与复权因子更新 | 收益和现金持仓账一致，未来事件不改变过去特征 |
| 改动未来行情、后来的财报/参考数据修订 | 过去的特征、资格、已发布信号和拟合参数不变化 |
| 5/20 日标签跨切分边界或尚未成熟 | 对应训练样本被正确清理，终测不参与拟合 |
| 日线缺失、真实停牌、长停牌后的恢复 | 日期对齐不压缩，不生成伪造原始成交记录 |
| 涨跌限制、成交单位、现金不足、可卖数量限制 | 订单与账户按适用规则执行，成本和未成交均可核对 |
| 重复运行、进程重启、模型切换或数据迟到 | 同一信号/订单不重复记账，新版本单独登记，旧版本可追溯 |

P1/P2 先用少量跨板块、含 ST/摘帽/退市/分红事件的真实样例验证，再扩展到全范围。少量样例只证明工程行为，策略结论必须使用冻结后的完整研究范围。

10. **按证据分开验收工程与研究结果。**

| 验收层 | 完成标准 | 未通过时的处理 |
|---|---|---|
| 数据可信 | 训练样本身份、必要价格/标签可核验；ST 可未知且原样记录；非 ST 回测另验状态覆盖；覆盖限制明确 | 输出缺口和影响，继续工程贯通；不宣称全 A 股效果 |
| 工程可复现 | 固定快照可重跑；泄漏测试和账户对账通过；信号、模型和订单有完整血缘 | 修复工程后重新生成受影响结果 |
| 模型值得继续 | 在冻结风险和成本假设下，至少 3 个开发测试年度中有 2 个对基线显示正的净收益增量；多种子与压力结果不由单次运行主导 | 保留有效基线，记录 Transformer 未提供足够增量 |
| 终测支持结论 | 封存区间的增量、回撤与时间块区间估计支持前述判断，费用加倍与执行延迟后仍有可解释的价值 | 区间跨零或主要依赖单一行情时标为证据不足；不据此调参后沿用同一终测名义 |
| 模拟流程可信 | 连续留痕、异常恢复、模型版本与预测成熟统计正确；拟定回撤预算在 P0 冻结并持续监控 | 停止新增模拟风险，修复问题，保留异常期间记录 |

上述“2/3 年度”是拟定的研究筛选门槛，不是稳定收益的统计证明。最终判断同时看效应大小、置信区间、市场暴露、实际成本和样本量。若 Transformer 单独未胜出，但与基线融合后通过同样验证，可以保留融合方案；若没有稳定增量，研究仍以有效的否定结果交付。

P0 需要按可承受回撤确定具体风险预算，并在查看策略结果前冻结。当前缺少用户实际资金和风险预算，100 万元等参数仅是可替换的模拟研究默认值，不能转成实盘仓位建议。

11. **算力、存储和费用先测量，再扩大。**

先在小样本上测量读数速度、单 epoch 时间、峰值内存/显存、全范围推理时间和导出费用，然后估算完整运行。可用本机 CPU 或支持的加速后端进行工程检查；完整训练以单张 16–24 GB 显存设备作为待验证候选，是否需要租用由实测决定，本计划不预设实际硬件或采购。

容量示例仅用于说明实现选择：假设 5,000 只证券、2,500 个交易日、40 个 float32 特征，连续基础数组约 2 GB；若把全部 60 日重叠窗口复制落盘，特征主体就约 120 GB，尚未计入其他元数据。实际股票数和数据密度以 P0 为准。采用列式存储、按需切片和批次加载，避免物化所有重叠窗口。

工期与费用主要受多年行情、退市资料、公司行动核验影响；历史 ST 作为非 ST 回测的独立依赖补齐。优先复用已保存的原始对象和规范化表，只修复缺失区间；研究训练读取冻结导出，避免每个 epoch 重复请求在线行情或湖仓服务。

12. **第一个迭代从以下五项交付开始。**

1. 给出按沪/深/北、板块、年份拆分的实际证券数量与日线覆盖报告，区分当前数量和历史数量。
2. 优先确认退市和公司行动来源的权限、完整性及可得时间；历史 ST 与撤销 ST 作为独立补充任务留痕。
3. 冻结研究范围、信息截面、5/20 日标签、模拟资金、风险预算、成本表和开发/封存时间分区。
4. 生成包含 ST 的日期训练池及固定研究数据 manifest，风险状态、所有隔离和排除原因可查；非 ST 模拟候选另行核验。
5. 完成若干已知 ST/摘帽、退市、代码变更和分红事件的独立对账，再进入特征与基线开发。

工程实现、合成样例回测和真实八股训练已经启动。本轮完成 Rust 长历史采集、跨来源价格/日历核对、训练专用快照、停牌/缺失/退市标签分类，详见 [本轮验收](quant-research/docs/p1-long-history.md)。训练可先于成交规则和历史行业数据验收运行；相应快照禁止进入组合模拟。全市场覆盖、正式多年训练、模拟调度及部署以开发状态为准，不以计划表代替实际验收。


2026-09-11 执行修订：用户允许部分不严谨数据后，将遗留 220 个价格边界全部分级纳入单独近似研究候选。采用跨供应方增量对比、参考价公式、明确容差、保留来源和单项缺失因子估算；不要求公告作为唯一凭据。逐事件保留方法、来源和默认研究权重，严格历史不改写；股东权益标签独立处理，旧训练快照冻结。后续通过严格版本与近似版本的独立结果比较评估影响，不将默认权重解释为概率。详见 [220 个遗留边界处理](quant-research/docs/research-estimates-20260911.md)。
