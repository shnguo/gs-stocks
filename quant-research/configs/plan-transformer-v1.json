{
  "protocol_id": "plan-transformer-v1",
  "purpose": "Step 4: sufficiently trained temporal plan model on the frozen six plan-value cohorts",
  "seed": 17,
  "device": "mps",
  "width": 48,
  "layers": 2,
  "attention_heads": 4,
  "lookback": 60,
  "batch_size": 512,
  "learning_rate": 0.0003,
  "max_epochs": 40,
  "min_epochs": 8,
  "patience": 6,
  "min_delta": 0.00000001,
  "price_auxiliary_weight": 0.1,
  "objective": "Equal plan/head/date loss: three binary heads and two training-standardized squared-error heads; date-weighted OHLC pinball auxiliary scaled by 0.05",
  "selection": "Independent selection-partition conditional net-return MSE, date and plan equally weighted; no calibration or evaluation outcomes used",
  "input": "60 historical sessions x 27 original features; standardization fit on unique training-window tokens only",
  "comparison": "Full identical stock/date/plan label cohorts versus frozen learned27; a pipeline comparison because temporal input differs",
  "calibration": "Same independent 12-date calibration as parent; retain raw and calibrated heads",
  "promotion": false,
  "sealed_holdout_start": "2025-08-07",
  "epoch_cap_policy": "Report cap-limited runs explicitly; do not call them converged; extend separately if selection still improves"
}
