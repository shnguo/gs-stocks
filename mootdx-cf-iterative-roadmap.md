# mootdx-cf 迭代式路线图

## 实施状态

截至 2026-08-20：

- 迭代 0 已完成：图表日线合同、治理登记、黄金响应样本和操作语义已经落地。
- 迭代 1 已完成：被拒绝的备用响应会保存并隔离；比较源传输失败会留下明确事件；Workflow 未知错误不会误报为已有任务。
- 迭代 2 已完成代码实现：图表接口按来源优先级和 as_of 为每个交易日选择一个正式修订。
- 迭代 3 已完成代码实现：none、qfq 和 hfq 查询已接入；价格复权不改变实际成交量；缺失因子会显式返回缺失状态。
- 迭代 4 正在执行：30 只跨市场板块样本和自动验收工具已经生成；隔离 preview 已完成 4 只股票、各 250 个交易日、none、qfq、hfq 三种口径共 12 项真实 Cloudflare 链路验收。网页已接入这 4 只股票，并完成 1 只股票 250 日序列的浏览器逐日指纹对账。北交所样本暴露了腾讯源无数据的覆盖缺口。30 只股票和独立数据对照仍未完成。
- 迭代 5 尚未开始：全 A 股回填、连续七个交易日观察和网页默认数据源切换必须等迭代 4 全部退出条件通过。
- 迭代 6 尚未开始：公开再分发仍受数据源条款审查约束。

## 2026-08-19 真实预览验收记录

- 预览环境使用独立 D1、R2 Lake、Collector、Orchestrator、ingestion Queue 和 DLQ，未消费或删除旧预览队列积压。
- 旧 market.fact_daily_bar 同时包含两种分区规范，当前 R2 SQL 会拒绝跨分区规范查询。为保留旧数据，预览读取和新写入切换到干净的 market_v2 命名空间，没有清空旧表。
- 600000 的真实日线任务完成 60 条发布；前复权和后复权因子分别完成 60 条发布。三种口径在同一 as_of 截面均返回 60 条，自动校验无错误。
- 复权因子写入曾因小数位超过 Iceberg Decimal 的 12 位标度失败。实现已改为确定性的 12 位舍入，并通过保留原始对象回放完成发布，回放没有再次请求数据源。
- Collector 预览已升级到容器版本 19，镜像摘要为 sha256:2604d2c28dce0e3dfb377e11972c5d18c04e1f98eb47df0689ebf8eae57df308，健康实例为 1。
- R2 SQL 延迟存在明显抖动，15 秒上限会让 hfq 偶发超时。服务端上限已调整为有界的 30 秒并补测试，但 250 日未缓存 1.5 秒目标仍未通过，不能视为性能验收完成。
- D1 迁移 0013 已将发布成员关系绑定到仓库、命名空间、表和显式代际。旧的 1424 个桶、25691 个修订完整复制到 legacy 范围；000001 使用既有原始运行回放时 source_attempts 为 0，并向 market_v2 代际发布 61 个修订，图表接口随后通过 61 条 none 日线校验。

## 2026-08-20 五只股票受控波次

- 600036、688008、000333、300059 的日线任务各发布 250 个修订；前复权和后复权任务各发布 250 个修订。两次空的腾讯因子响应由有界重试恢复，首次失败原因仍保留在任务审计记录中。
- 430047 在五次有界尝试后进入 failed 终态，明确记录 selected source tencent produced no bars。该结果按数据源覆盖缺口处理，不生成空蜡烛或伪造成功。
- 以 2026-08-20T07:45:00Z 为 as_of，4 只已入湖股票在 none、qfq、hfq 三种口径下各返回 250 条，共 12 项自动验收全部通过；无重复日期、OHLC 约束、质量状态、复权语义和原始血缘问题。
- 有界扫描上限从 366 个自然日调整为 400 个自然日，使最近 250 个 A 股交易日可以完整覆盖，同时继续拒绝无界查询。
- Collector 预览已升级到版本 6c73663b-e294-41e0-b92a-c6f4af00803d，镜像摘要为 sha256:5ca00befd32d9d8d22e9c7098959c662c28e9bc41d7872cbc55929e02263dd24，健康检查通过。
- stock-indicator-dashboard 已删除默认模拟行情，改由服务端代理读取正式投影。客户端可切换 4 只股票、none、qfq、hfq 和 60、120、250 日窗口，并显示来源、对照状态、as_of 和 data_version；Bearer Token 不下发浏览器。
- 浏览器实际绘制的澜起科技前复权 250 条 OHLCV 指纹为 7669baaa，与网页 API 对同一序列计算的指纹完全一致。股票切换、不复权切换、250 根蜡烛和 250 个指标点通过；桌面和 390 像素移动布局通过，浏览器控制台无警告或错误。
- 本机 workerd 直连 workers.dev 会稳定超时，因此本地验收使用仅监听 127.0.0.1、经 SOCKS5 访问上游的开发代理。Cloudflare 生产部署仍使用服务端 Secret 和直接 Worker 网络，不依赖该代理。
- 正确性验收已经扩大，但 R2 SQL 未缓存 1.5 秒目标、独立参考源对照、30 只样本和其余样本的浏览器逐日对账仍是迭代 4 的阻断项。

## 目标

把 mootdx-cf 从可审计、可回放的 A 股湖仓，逐步交付为能够稳定支撑日线蜡烛图和量价指标的真实数据源。

第一阶段只承诺沪深北 A 股日线，不把港股、美股、实时行情或公开再分发纳入首个可用版本。

## 首个可用版本的验收合同

- 每只股票、每个交易日最多返回一条正式行情。
- 明确区分不复权、前复权和后复权，默认由客户端显式选择。
- 每条记录保留来源、修订、抓取时间、质量状态和原始对象血缘。
- 没有可用第二来源时必须明确标记，不允许静默视为交叉验证成功。
- 被拒绝或解析失败的源响应必须先保存，再进入隔离区。
- 停牌日不伪造零成交蜡烛；交易日缺失和停牌需要不同状态。
- 最近 250 个交易日的图表接口，未缓存响应目标为 1.5 秒以内，缓存响应目标为 300 毫秒以内。
- 已收盘交易日的正式数据，目标是在北京时间 18:00 前完成，月度可用率不低于 99.5%。

## 迭代方法

每个迭代控制在一周左右，并且必须产生一个可以独立验证的增量。每轮结束执行同一个闭环：

1. 演示真实输入到真实输出。
2. 核对数据质量、延迟、完整性和成本。
3. 记录未通过项和新发现风险。
4. 只把影响下一轮目标的事项加入下一轮。
5. 保留可回退的上一版合同和数据投影。

## 迭代 0：冻结图表数据合同

### 目标

在修改数据链路前，先定义网页真正需要的数据产品，避免直接暴露事实表。

### 工作项

- 定义 chart-daily-bars v1 合同和接口响应字段。
- 明确正式日线的唯一选择规则：质量状态、来源优先级、修订时间和查询时点。
- 定义 as_of 的点时语义，避免历史查询看到未来修订。
- 明确 none、qfq、hfq 三种复权方式。
- 明确成交量使用实际成交股数，复权成交量如需提供必须单独命名。
- 建立黄金样本：沪市、深市、北交所、停牌、除权、源间差异、修订更正各至少一个案例。

### 退出条件

- 同一个输入快照只能产生一种正式日线结果。
- 产品合同能够表达来源不可用、源间冲突、停牌和数据缺失。
- 前端不需要理解 Iceberg 修订表的内部结构。

## 迭代 1：修复证据保存和降级语义

### 目标

先让错误变得可信、可观察、可回放，再扩大数据量。

### 工作项

- 备用源没有可用行情时，仍返回并保存已经收到的原始响应。
- 所有被拒绝的尝试进入隔离区，并记录解析失败原因。
- 为交叉验证增加 matched、mismatch、unavailable、timeout 状态。
- 明确没有第二来源时是继续发布、降级发布还是阻断发布。
- Workflow 只对明确的重复实例错误执行查询或重启，其他错误保留原始原因。
- 增加失败路径测试：空响应、畸形响应、超时、两个来源同时失败和重复任务。

### 退出条件

- 任何收到响应字节的尝试都能追溯到原始对象或隔离记录。
- 数据源不可用不会被记录成交叉验证成功。
- Workflow 权限、配额和服务异常不会被误报为任务已存在。

## 迭代 2：交付唯一的正式日线投影

### 目标

让蜡烛图接口每个交易日只返回一条确定、可解释的记录。

### 工作项

- 建立 curated_daily_bar 投影或等价查询层，不直接把 fact_daily_bar 暴露给图表。
- 按股票、交易日、查询时点选择通过质量门的正式修订。
- 来源选择遵循合同中的优先级，源间冲突保留质量状态。
- 历史接口和 D1 最新值使用同一套选择规则。
- 添加唯一日期、稳定排序、分页边界和修订回放测试。
- 返回结果级数据版本或内容哈希，支持浏览器缓存和结果复现。

### 退出条件

- 任意股票的返回结果不存在重复交易日。
- 相同 as_of 和相同数据快照产生完全相同的响应。
- 修订不会覆盖旧事实，但图表只看到当时应当可见的正式版本。

## 迭代 3：交付复权和指标就绪数据

### 目标

使网页能够安全计算 MA、ATR 和主力吸货等量价指标。

### 工作项

- 图表接口支持 none、qfq、hfq。
- 返回 adjustment_basis、adjustment_revision 和调整基准日期。
- 复权结果绑定精确的原始日线快照和调整因子快照。
- 价格复权与成交量语义分开，禁止隐式改变成交量单位。
- 对除权日前后建立连续性测试，同时保留不复权价格的真实跳空。
- 在网页端只基于完整、按日期升序的正式序列计算指标。
- 指标图展示数据状态，源间冲突或缺失窗口不得伪装成正常值。

### 退出条件

- 黄金样本的前复权结果与人工核算一致。
- 公司行动不会在指标中制造错误的新低或异常波动。
- 同一数据版本和公式版本可以重现完全相同的指标序列。

## 迭代 4：真实数据小规模验收

### 目标

用真实供应商、真实 R2、D1 和浏览器验证完整链路。

### 工作项

- 在隔离的 preview 环境选择 30 至 50 只代表性股票。
- 覆盖沪深北、ST、停牌、近期除权、高价股、低价股和源间已知差异。
- 对最近 250 个交易日执行小规模回填。
- 将 TDX、腾讯和一个独立参考源按日期对比 OHLC、成交量和成交额。
- 验证抓取、原始保存、解析、质量、发布、投影、API、网页和指标全链路。
- 记录差异分布，不以 HTTP 成功代替数据正确性。

### 退出条件

- 样本范围内没有重复日期、未来日期或单位错误。
- 所有差异都有来源、阈值和处理结论。
- 浏览器实际展示的蜡烛与接口响应逐日一致。
- 主力吸货指标能够用相同输入在测试和浏览器中复现。

## 迭代 5：全 A 股日常运行

### 目标

从小样本扩展到全 A 股，并证明系统能够连续运行和自愈。

### 工作项

- 分交易所、分批次扩大到完整股票池。
- 启用收盘后调度、队列、死信队列、修复任务和断点续跑。
- 建立新鲜度、缺失交易日、隔离量、源间差异、任务延迟和成本监控。
- 设定熔断条件，异常扩大时停止发布而不是继续污染正式投影。
- 连续运行七个交易日，再决定是否切换网页默认数据源。
- 保留旧数据源作为短期只读回退，直到新链路完成观察期。

### 退出条件

- 连续七个交易日达到完整性和新鲜度目标。
- 任意失败股票可以单独修复，不需要清空共享队列。
- 正式投影能够从湖仓重新构建。
- 网页切换和回退都经过实际演练。

## 迭代 6：生产化与公开服务决策

### 目标

在内部数据源稳定后，再决定是否对外提供服务。

### 工作项

- 完成各数据源使用条款、缓存、保留、署名和再分发审查。
- 只有明确批准的数据集才能进入公开导出或公共 API。
- 完成密钥轮换、最小权限、成本预算、告警和恢复演练。
- 定义 API 版本、废弃周期、配额、服务目标和使用文档。
- 根据真实使用量决定是否需要 KV、额外缓存层或预生成图表响应。

### 退出条件

- 私有分析和公共再分发的权限边界可执行、可审计。
- 从空环境恢复控制面和正式投影的演练通过。
- 成本和服务目标由实际运行数据支持，不依赖估算。

## 后续可选路线

以下项目不应阻塞 A 股日线首个可用版本：

- 港股：新增 XHKG、HKD、香港时区、交易日历、代码和公司行动语义。
- 美股：新增交易所、USD、盘前盘后、拆股和时区处理。
- 分钟行情：单独设计延迟、快照、限流和存储成本目标。
- 服务端指标：只有在需要跨客户端统一计算时再增加，首版优先让 API 提供可信输入。
- 公开数据 API：必须在授权审查完成后再启动。

## 当前优先级

1. 五只股票受控波次已完成，其中四只通过全部口径、北交所一只暴露源覆盖缺口；下一步修复或替换北交所来源，再扩展到 30 只股票并加入独立参考源对照。
2. 分别测量 R2 SQL 冷热查询延迟；1.5 秒目标未通过前，不扩大为网页默认数据源。
3. 接入蜡烛图和主力吸货指标网页，完成接口逐日对账、浏览器交互和视觉验收。
4. 小样本全部通过后，再进行全 A 股回填和连续七个交易日观察；公共服务、其他市场和分钟行情继续后置。
